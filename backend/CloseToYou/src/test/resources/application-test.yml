spring:
  datasource:
    url: jdbc:mysql://i11b201.p.ssafy.io:3306/closetoyoutest?serverTimezone=UTC
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: closetoyou123!

  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.MySQLDialect

logging:
  level:
    org.hibernate.SQL: debug
